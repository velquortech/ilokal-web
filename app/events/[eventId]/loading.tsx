import { BusinessProfileSkeleton } from '@/components/customer/skeletons';

// Same shape as a shop page: hero image, heading block, body sections.
export default function Loading() {
  return <BusinessProfileSkeleton />;
}
